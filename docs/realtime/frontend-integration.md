# Frontend Dashboard Integration

React components combine initial REST baseline state (`/dashboard/summary`) with live WebSocket stream updates via `useWebSocket` hook, appending live events, toast alerts, and connection badges without page refreshes.
