# Structured Playbook Models & Conditions

Playbooks define automated workflows triggered by event/alert criteria. Triggers use safe structured comparison operators (`gte`, `lte`, `eq`, `ne`, `in`, `contains`) evaluated without dynamic Python code execution.
